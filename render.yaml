services:
  - type: web
    name: second-listen
    runtime: python
    plan: free
    rootDir: app
    buildCommand: python -m compileall -q .
    startCommand: python deployment/browser/server.py
    healthCheckPath: /health
    envVars:
      - key: ASSEMBLYAI_API_KEY
        sync: false
      - key: HOST
        value: 0.0.0.0
      - key: AGENT
        value: second-listen
      - key: MODE
        value: inline
      # Public contest demo: each Voice Agent session is at most five minutes.
      - key: VOICE_SESSION_MAX_DURATION_SECONDS
        value: '300'
      - key: DATA_DIR
        value: /var/data
      - key: REQUIRE_HTTPS
        value: '1'
